name: mqtt-explorer

project_files:
  - docker-compose.mqtt-explorer.yaml
  - commands/host/mqtt-explorer

ddev_version_constraint: '>= v1.24.10'

post_install_actions:
  - |
    echo ""
    echo "MQTT Explorer has been installed."
    echo ""
    echo "Use 'ddev mqtt-explorer' to launch the MQTT Explorer web UI."
